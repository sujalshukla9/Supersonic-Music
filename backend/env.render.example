# ============================================
# Supersonic Music Backend - Render.com Environment Variables
# ============================================
# Copy these to: Render Dashboard → Your Service → Environment
# ============================================

# --------------------------------------------
# REQUIRED VARIABLES (Set by Render automatically)
# --------------------------------------------
# PORT - Render sets this automatically, DO NOT add this manually!

# NODE_ENV - Set automatically to 'production' by render.yaml
NODE_ENV=production

# YOUTUBE_DL_SKIP_DOWNLOAD - Skips binary download during npm install
# Set to "true" to prevent GitHub API rate limiting issues on Render
# The yt-dlp binary is installed via pip in the build command instead
YOUTUBE_DL_SKIP_DOWNLOAD=true

# --------------------------------------------
# OPTIONAL VARIABLES
# --------------------------------------------

# YouTube Data API Key
# Required for: /related endpoint (related songs feature)
# Not required for: /audio, /stream (core playback will work without it)
# 
# How to get a key:
# 1. Go to https://console.cloud.google.com
# 2. Create/select a project
# 3. Enable "YouTube Data API v3"
# 4. Go to Credentials → Create Credentials → API Key
# 5. (Optional) Restrict the key to YouTube Data API only
#
YT_KEY=your_youtube_api_key_here
